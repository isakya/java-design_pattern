# 单一职责原则
1. 每个方法、每个类、每个框架都只做一件事情。
    比如：
   + a. Math.round()，只复制完成四舍五入的功能。
   + b. Reader类，只负责读取文本文件（类）。
   + c. SppringMVC，只负责简化MVC的开发（框架）。


2. 代码规范：方法的语句在同一个抽象层次上，不能又抽象又有实现细节
注解： plus文件是单一职责原则的例子，原文件是反例   
